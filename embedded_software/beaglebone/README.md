Hackerboat Navigator
====================

This is the navigation control software for the beaglebone.

It is compatible with [qgroundcontrol](http://qgroundcontrol.org).

Run `make` to compile and `make doxy` to generate documentation.